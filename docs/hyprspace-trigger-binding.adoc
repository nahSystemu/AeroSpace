= hyprspace-trigger-binding(1)
include::util/man-attributes.adoc[]
:manname: hyprspace-trigger-binding
// tag::purpose[]
:manpurpose: Trigger HyprSpace binding as if it was pressed by user
// end::purpose[]

// =========================================================== Synopsis
== Synopsis
[verse]
// tag::synopsis[]
hyprspace trigger-binding [-h|--help] <binding> --mode <mode-id>

// end::synopsis[]

// =========================================================== Description
== Description

// tag::body[]
{manpurpose}

You can use hyprspace-config command to inspect available bindings: +
`hyprspace config --get mode.main.binding --keys`

// =========================================================== Options
include::util/conditional-options-header.adoc[]

-h, --help:: Print help
--mode <mode-id>:: Mode to search `<binding>` in

// =========================================================== Arguments
include::util/conditional-arguments-header.adoc[]

<binding>:: Binding to trigger

// =========================================================== Examples
include::util/conditional-examples-header.adoc[]

* Run alphabetically first binding from config (useless and synthetic example): +
`hyprspace trigger-binding --mode main "$(hyprspace config --get mode.main.binding --keys | head -1)"`
* Trigger `alt-tab` binding: +
`hyprspace trigger-binding --mode main alt-tab`

// end::body[]

// =========================================================== Footer
include::util/man-footer.adoc[]
